runtime: python39

env: standard
instance_class: F1

automatic_scaling:
  min_idle_instances: automatic
  max_idle_instances: 1
  min_pending_latency: 3000ms
  max_pending_latency: automatic
  max_instances: 2

handlers:
- url: /static
  static_dir: webapp/dist/static

- url: /.*
  script: auto